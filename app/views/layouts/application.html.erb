<!DOCTYPE html>
<html lang="en">
  <head>
    <link href='https://fonts.googleapis.com/css?family=Muli' rel='stylesheet' type='text/css'>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "California Election Costs" %></title>
    <%= csrf_meta_tags %>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <!-- For third-generation iPad with high-resolution Retina display: -->
    <!-- Size should be 144 x 144 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-144x144-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '144x144' %>
    <!-- For iPhone with high-resolution Retina display: -->
    <!-- Size should be 114 x 114 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-114x114-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '114x114' %>
    <!-- For first- and second-generation iPad: -->
    <!-- Size should be 72 x 72 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-72x72-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png', :sizes => '72x72' %>
    <!-- For non-Retina iPhone, iPod Touch, and Android 2.1+ devices: -->
    <!-- Size should be 57 x 57 pixels -->
    <%= favicon_link_tag 'apple-touch-icon-precomposed.png', :rel => 'apple-touch-icon-precomposed', :type => 'image/png' %>
    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>
    <%= javascript_include_tag "application" %>
  </head>
  <body>
    <div id ="mybody">
      <div class="navbar navbar-default navbar-fixed-top">
        <div class="container">
          <div class="mynavbar">
            <div class="mynav_login navbar-link"> 
              <% if allow? :users, :logout %>
                Signed in as <%= current_user.username %> with <%= link_to "#{ current_user.county.name }.", profile_user_path(session[:user_id]) %><span style="width:10px;q"></span>
                  <%= link_to "Log Out", logout_path, { class: "btn btn-default"} %>
              <% else %>
                <%= link_to "Sign Up", signup_path %> or
                <%= link_to "Log In", login_path %>
              <% end %>
            </div>

            <% if current_user && current_user.observer? %>
            <% elsif current_user %>
              <a class="navbar-brand" href="<%= profile_user_path(current_user.id) %>"><img id="caceo-logo" src="/images/CACEO_logo.gif" alt="logo" /></a>
            <% else %>
              <a class="navbar-brand" href="/"><img src="/images/CACEO_logo.gif" alt="logo" /></a>
            <% end %>


            <button type="button" class="navbar-toggle collapsed" data-target="#mynav" data-toggle="collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
              
            <div class="collapse navbar-collapse" id="mynav">

              <ul class="nav navbar-nav list-unstyled">
                <% if current_user && current_user.observer? %>
                  <div class="observer_county_select">
                    <%= form_for(current_user, method: :patch) do |f| %>
                      <%= f.collection_select(:county_id, County.all, :id, :name) %>
                      <%= submit_tag "Change County" %>
                    <% end %>
                  </div>
                <% end %>

                <% if allow? :announcements, :index %>
                  <li>
                    <%= link_to 'FAQs', faqs_path %>
                  </li>
                <% end %>

                <% if allow? :faqs, :index %>
                  <li>
                    <a href=<%= announcements_path %>>Announcements
                      <% announcement_count = Announcement.count_new_for(current_user) %>
                      <% if announcement_count > 0 %>
                        <span class="badge"> <%= announcement_count %> </span>
                      <% end %>
                    </a>
                  </li>
                <% end %>

                <% if allow? :categories, :index %>
                  <li class="dropdown">
                    <a class="dropdown-toggle" id="dLabel" role="button" data-toggle="dropdown" data-target="#" href="/page.html">
                      Surveys
                      <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                      <%= render partial: "layouts/surveys_nav", locals: { session: session } %>
                    </ul>
                  </li>
                <% end %>

                <% if allow? :activities, :index %>
                  <li class="dropdown">
                    <a class="dropdown-toggle" id="dLabel" role="button" data-toggle="dropdown" data-target="#" href="/page.html">
                      Admin Tools
                      <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                      <li class="mynav_li"><%= link_to "Daily Activity", activities_path(day: true)%></li>
                      <li class="mynav_li"><%= link_to "Weekly Activity", activities_path(week: true)%></li>
                      <li class="mynav_li"><%= link_to "All Activity", activities_path(all: true)%></li>
                      <li class="divider"></li>
                      <li class="mynav_li"><%= link_to "Surveys Report", activities_path(full_report: true, format: :xlsx)%></li>
                      <li class="mynav_li"><%= link_to "Counties Report", activity_path(id: 'county', format: :xlsx)%></li>
                    </ul>
                  </li>
                <% end %>

                <% if allow? :users, :profile %>
                  <li class="dropdown">
                    <a class="dropdown-toggle" id="dLabel" role="button" data-toggle="dropdown" data-target="#" href="/page.html">
                      Account
                      <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu" role="menu" aria-labelledby="dLabel">
                      <li class="mynav_li"><%= link_to "Home",  profile_user_path(session[:user_id]) %></li>
                      <li class="mynav_li"><%= link_to "Log Out", logout_path %></li>
                      <li class="divider"></li>
                      <li class="mynav_li"><a href="/messages/new">Contact Us</a></li>
                    </ul>
                  </li>
                <% end %>
              </ul>
            </div><!--/.navbar-collapse -->
          </div>
        </div>
      </div>
      <div class="container">
        <div class="row">
          <div class="">
            <%= flash_messages %>
            <%= yield %>
          </div>
        </div><!--/row-->
        <div class="myfooterpad">
        </div>
        <div class="myfooter">
          <footer>
            &copy; California Association of Clerks and Election Officials <strong>www.caeo58.org</strong>
          </footer>
        </div>
      </div>
    </div>
  </body>
</html>
