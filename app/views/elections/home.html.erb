
<h2 class ="myh2yr">Direct Cost Categories Year Overview for <%= current_user.county.name %></h2>
<div class ="instructs"> (Click on a Year, then on an Election to Begin)</div>

<table class="table table-condensed" style="border-collapse:collapse;">
  <colgroup>
    <col width="35%"></col>
    <col width="40%"></col>
    <col width="10%"></col>
    <col width="15%"></col>
  </colgroup>
  <thead>
    <tr>
      <th>Election</th>
      <th>Percent Complete</th>
      <th></th>
      <th>Direct Costs</th>
    </tr>
  </thead>
    <tbody>
    <% @election_years.each do |election_year| %>
      <tr>
        <td><%= link_to election_year.year, election_year_categories_path(election_year) %></td>
          <td>
            <div class="progress election-year">
            <div class="progress-bar" 
              role="progressbar" 
              aria-valuenow=<%="#{election_year.percent_answered_direct_cost( current_user )}"%> 
              aria-valuemin="0" 
              aria-valuemax="100" 
              style="<%="width: #{election_year.percent_answered_direct_cost( current_user )}%;" %>">
              <% if election_year.surveys_open?(current_user) %>
                <%= election_year.percent_answered_direct_cost( current_user ) %>%
              <% else %>
              <% end %>
            </div> 
            </div>
          </td>
          <td></td>
          <td>
            <% if election_year.surveys_open?(current_user) %>
              $<%= number_with_delimiter election_year.total_direct_cost_for( current_user ) %>
            <% else %>
              <span class="text-muted">Not Started<span>
            <% end %>
          </td>      
      </tr>
    <% end %>
  </tbody>
</table>

<h2 class ="myh2yr_tot"> TOTAL DIRECT COSTS FOR ALL ELECTIONS: $<%= number_with_delimiter @election_years.inject(0) {|memo, y| memo + y.total_direct_cost_for( current_user ) } %></h2>
