<div class="panel panel-default">
<table class="table">
  <% values.each do |v| %>
  <tr>
    <td><%= v.question.label %></td>
    <% if v.question.multi_select? %>
      <td><%= v.survey_response.response.send(v.question.field + '_multi_select').to_s.gsub("\"", '').chop.gsub('[', '') %></td>
    <% else %>
      <td><%= v.value %></td>
    <% end %>
  </tr>
  <% end %>
</table>
</div>