
<h2 class ="myh2yr"><%= "Edit #{ @survey.title} for the #{ @survey_response.election.year }" %>
&rarr;  Step <%= @response_form.current_step + 1 %> of <%= @response_form.total_steps %></h2>

<%= render partial: 'survey_responses/survey', locals: { survey: @survey } %>

<div class="end_link">
  <%= link_to "Back to Response Summary", survey_response_path( @survey_response.id ) %>|
  <%= link_to "Surveys for #{ @response_form.model.election.year }", 
    election_surveys_path( @response_form.election_id ) %> |
    <%= link_to "All Elections", elections_path  %>
</div>